# Skippy SAP EWM Assistant - Environment Configuration
# Copy this file to .env and update the values for your deployment

# =============================================================================
# AI Configuration - Choose either OpenAI OR Azure OpenAI (not both)
# =============================================================================

# --- Standard OpenAI Configuration ---
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- Azure OpenAI Configuration (Alternative to standard OpenAI) ---
# Uncomment and configure these if using Azure OpenAI instead of OpenAI
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
# AZURE_OPENAI_API_VERSION=2023-12-01-preview
# AZURE_OPENAI_DEPLOYMENT_NAME=your-gpt-deployment-name
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002

# Default OpenAI model to use (options: gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview, gpt-4o)
OPENAI_MODEL_DEFAULT=gpt-3.5-turbo

# Default temperature for AI responses (0.0 = focused, 1.0 = creative)
OPENAI_TEMPERATURE_DEFAULT=0.7

# Maximum tokens for AI responses
OPENAI_MAX_TOKENS=2000

# =============================================================================
# ChromaDB Vector Database Configuration
# =============================================================================
# Path to ChromaDB database directory (relative to project root)
CHROMA_DB_PATH=./chroma

# Name of the ChromaDB collection
CHROMA_COLLECTION_NAME=sap_ewm_docs

# Disable ChromaDB telemetry for enterprise environments
# This prevents ChromaDB from sending usage data to external servers
ANONYMIZED_TELEMETRY=false

# =============================================================================
# Application Configuration
# =============================================================================
# Default number of context documents to retrieve per query
DEFAULT_CONTEXT_DOCS=3

# Maximum conversation history messages to keep in memory
MAX_CONVERSATION_HISTORY=6

# Directory for PDF data files
DATA_DIRECTORY=./data

# Directory for application assets (avatars, images, etc.)
ASSETS_DIRECTORY=./assets

# =============================================================================
# Embedding Model Configuration
# =============================================================================
# Sentence transformer model for embeddings (default: all-MiniLM-L6-v2)
# Options: all-MiniLM-L6-v2, all-mpnet-base-v2, multi-qa-MiniLM-L6-cos-v1
EMBEDDING_MODEL=all-MiniLM-L6-v2

# =============================================================================
# Document Processing Configuration
# =============================================================================
# Minimum tokens per text chunk
MIN_TOKENS=500

# Maximum tokens per text chunk  
MAX_TOKENS=1000

# Token overlap between chunks
TOKEN_OVERLAP=100

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log directory
LOG_DIRECTORY=./logs

# =============================================================================
# Deployment Configuration (Platform-specific)
# =============================================================================

# For Streamlit Cloud deployment:
# Just upload the repository and ensure .env contains the required variables

# For Railway deployment:
# Add these as environment variables in your Railway project dashboard
# OPENAI_API_KEY=your-key-here

# For Docker deployment:
# ENV OPENAI_API_KEY=your-key-here
# ENV CHROMA_DB_PATH=/app/chroma
# ENV DATA_DIRECTORY=/app/data
# ENV ASSETS_DIRECTORY=/app/assets

# For Heroku deployment:
# heroku config:set OPENAI_API_KEY=your-key-here
# heroku config:set CHROMA_DB_PATH=./chroma

# =============================================================================
# Security Notes
# =============================================================================
# - Never commit the actual .env file to version control
# - Keep your OpenAI API key secure and rotate it regularly
# - Use environment-specific configurations for development/staging/production
# - Monitor API usage to avoid unexpected charges
# - Set up proper access controls for production deployments
